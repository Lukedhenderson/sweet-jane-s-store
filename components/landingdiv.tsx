export const revalidate = 0;

const Landing =  () => {

  return ( 
    <div className=" h-full w-full flex flex-col justify-center items-center text-center gap-y-8 mb-10 font-semibold">
      While I wait to turn this into a Llama retail empire, I figured I would make it Harvard themed. Your Item should be featured below. Make sure to use the test card!
    </div>
   );
}
 
export default Landing